#!/bin/bash
sed -i 's/using var httpClient = new System.Net.Http.HttpClient { Timeout = TimeSpan.FromSeconds(10) };/var handler = new System.Net.Http.HttpClientHandler();\n            if (UseDefaultCredentials)\n            {\n                handler.UseDefaultCredentials = true;\n            }\n            else\n            {\n                handler.Credentials = new System.Net.NetworkCredential(AuthUsername, AuthPassword, AuthDomain);\n            }\n            using var httpClient = new System.Net.Http.HttpClient(handler) { Timeout = TimeSpan.FromSeconds(10) };/' src/AzureDevOps.DesktopManager/ViewModels/MainWindowViewModel.cs
